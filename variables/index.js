/* Rules
    * Variables names cannot be reserved keywords (if, else, switch)
    * Should be descriptive names
    * Cannot start with a number (1name)
    * Cannot contain a space( ) or hyphen(-)
    * Case sensitive
*/

// By default variable define is JS, the value is UNDEFINED
let name;
console.log(name); 
